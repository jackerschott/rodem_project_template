api_key: null # will get set from snakemake
user: {{ cookiecutter.wandb_username }}
project: {{ cookiecutter.wandb_projectname }}
name: null # name of run, will get set from snakemake
save_dir: null # will get set from snakemake
tags:
  - ${stage}
